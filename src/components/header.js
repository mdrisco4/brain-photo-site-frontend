// import { Link } from "gatsby"
// import PropTypes from "prop-types"
import React from "react"

const Header = ({ siteTitle }) => (
  <div style={{
    backgroundColor: "rgb(113, 84, 58)",
    color: "rgb(216, 100, 52)",
    height: "100px",
  }}>
efhasdif
  </div>
)

export default Header
